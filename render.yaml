services:
  - type: static
    name: affiliate-landing-generator
    env: static
    buildCommand: npm install && npm run build
    publishPath: ./dist
    envVars:
      - key: GEMINI_API_KEY
        sync: false
